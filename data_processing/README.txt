1. for the Demographics spreadsheet run make sure all the gait data is in the current directory inside 'data', or otherwise modify your basepath inside the meta.py file, and then run the following command that generates the CSV file:

python meta.py

