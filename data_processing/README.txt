1. for the Demographics spreadsheet run make sure all the gait data is in the current directory inside 'data', or otherwise modify your basepath inside the Demographics.py file, and then run the following command that generates the CSV file:

python Demographics.py

2. for the Markers, EMG and ForcePlate spreadsheets run their corresponding .ipynb files.
